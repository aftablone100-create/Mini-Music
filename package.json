{
  "name": "mini-music-yt-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal server-side proxy that keeps the YouTube Data API v3 key off the Mini Music client.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
